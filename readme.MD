# Scuttlebutt Server in Docker

### Full Documentation

https://handbook.scuttlebutt.nz/guides/pubs/setup-a-pub

### Instructions

1. To build the server:
`./build.sh`
   
2. To run the server:
`./run.sh <server_host>`
